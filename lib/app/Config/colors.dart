import 'package:flutter/material.dart';

class AppColors {
  // Light theme colors
  static const Color lightPrimary = Color.fromARGB(255, 255, 255, 255);
  static const Color lightSecondary = Color.fromARGB(255, 0, 0, 0);

  // Dark theme colors
  static const Color darkPrimary = Color.fromARGB(255, 0, 0, 0);
  static const Color darkSecondary = Color.fromARGB(255, 255, 255, 255);
}
